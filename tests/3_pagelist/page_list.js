/**
 * Implement a function that takes a sitemap (nested object - see ./sitemap.js)
 * and returns an array of page titles in order as they are in the tree.
 */

export default function getPagesList(sitemap) {
  return sitemap
}
